# Copy this to .env.local and fill in your values
# Get DATABASE_URL from: Vercel Dashboard → Storage → Your Postgres DB → .env.local tab

DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require
